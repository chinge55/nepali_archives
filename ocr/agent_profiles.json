{
  "_comment": [
    "Public, provider-neutral capability defaults for the scanned-book DAG.",
    "The graph stores only strong_reader or fast_reader. Put any concrete",
    "agent/model bindings in ignored agent_profiles.local.json, or select a",
    "different file with OCR_AGENT_PROFILES. The packet prompt remains the",
    "authoritative task contract."
  ],
  "active": "default",
  "profiles": {
    "default": {
      "strong_reader": { "reasoning_effort": "high" },
      "fast_reader": { "reasoning_effort": "medium" }
    }
  }
}
